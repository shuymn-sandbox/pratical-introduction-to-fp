{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cabal run",
            "type": "shell",
            "command": "cabal",
            "args": ["run", "-v0", "${file}"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
